<?php
	if (empty($parameters) === true) {
		exit;
	}

	function _validateHostname($hostname, $allowIpAddress = false) {
		$response = false;
		// todo: validate hostnames with URLs and IP addresses
		return $response;
	}
?>
